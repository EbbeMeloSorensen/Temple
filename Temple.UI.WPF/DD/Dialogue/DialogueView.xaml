<UserControl x:Class="Temple.UI.WPF.DD.Dialogue.DialogueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dialogue="clr-namespace:Temple.ViewModel.DD.Dialogue;assembly=Temple.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance dialogue:DialogueViewModel}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Skins/MainSkin.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Row="0" Grid.Column="0" Source="{Binding Path=NPCPortraitPath, 
                        UpdateSourceTrigger=PropertyChanged, 
                        Converter={StaticResource ImagePathConverter2}}"/>

        <TextBlock Grid.Row="0" Grid.Column="1" FontSize="50" FontWeight="Bold" Margin="20, 0, 10, 10"
                   Text="{Binding Title}"/>

        <!--Deprecated-->
        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" FontSize="30" FontWeight="Bold"
                   Margin="0" Background="Gray" 
                   Text="{Binding Message}"/>

        <!--Deprecated-->
        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="20" Width="300" Height="100" FontSize="30" FontWeight="Bold"  Content="Take Quest"
                    Command="{Binding TakeQuest_Command}"
                    Visibility="{Binding TakeQuestPossible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            <Button Margin="20" Width="300" Height="100" FontSize="30" FontWeight="Bold" Content="Leave"
                    Command="{Binding Leave_Command}"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" FontSize="30" FontWeight="Bold"
                   Margin="0" TextWrapping="Wrap" 
                   Text="{Binding MessageNew}"/>

        <ItemsControl Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Background="Yellow"
                      ItemsSource="{Binding Options}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Width="50" Height="50" VerticalAlignment="Top" Margin="11"
                                Command="{Binding DataContext.SelectOption_Command,
                                    RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding OptionId}"/>
                        <TextBlock Grid.Column="1" FontSize="50" TextWrapping="Wrap"
                                   Text="{Binding Text}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

    </Grid>
</UserControl>
