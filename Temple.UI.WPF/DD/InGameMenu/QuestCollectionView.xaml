<UserControl x:Class="Temple.UI.WPF.DD.InGameMenu.QuestCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Temple.UI.WPF.DD.InGameMenu"
             xmlns:graph="clr-namespace:Craft.UIElements.Graph;assembly=Craft.UIElements"
             xmlns:inGameMenu="clr-namespace:Temple.ViewModel.DD.InGameMenu;assembly=Temple.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance inGameMenu:QuestCollectionViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <graph:GraphView Grid.Column="0" DataContext="{Binding GraphViewModel}"/>

        <ItemsControl Grid.Column="1" ItemsSource="{Binding Quests}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" FontSize="50" FontStyle="Italic"
                                   Margin="20,10,10,10" TextWrapping="Wrap"
                                   Text="{Binding Title}"/>
                        <Button Grid.Column="1" Width="100" Height="50" VerticalAlignment="Top"
                                Margin="11" Content="Cheat complete"
                                Command="{Binding DataContext.CheatCompleteQuest_Command,
                                    RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding Title}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
