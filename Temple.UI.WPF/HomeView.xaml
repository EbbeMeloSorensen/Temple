<UserControl x:Class="Temple.UI.WPF.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModel="clr-namespace:Temple.ViewModel;assembly=Temple.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" d:DataContext="{d:DesignInstance viewModel:HomeViewModel }">
    <Grid>
        <StackPanel>
            <Button Content="Smurfs.." HorizontalAlignment="Center" Width="120"
                    Command="{Binding GoToSmurfManagementCommand }"/>
            <Button Content="People.." HorizontalAlignment="Center" Width="120" Margin="0, 5, 0, 0"
                    Command="{Binding GoToPeopleManagementCommand }"/>
            <Button Content="Export State Machine" HorizontalAlignment="Center" Width="120" Margin="0, 5, 0, 0"
                    Command="{Binding ExportStateMachineCommand }"/>
            <Button Content="Exit Application.." HorizontalAlignment="Center" Width="120" Margin="0, 5, 0, 0"
                    Command="{Binding ShutdownCommand }"/>
        </StackPanel>
    </Grid>
</UserControl>
